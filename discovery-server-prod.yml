eureka:
  instance:
    hostname: ${DISCOVERY_SERVER_SVC}
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://${DISCOVERY_SERVER_SVC}:${DISCOVERY_SERVER_PORT}/eureka/